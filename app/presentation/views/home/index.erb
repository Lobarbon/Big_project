    <div class="col border-left border-right border-warning">
        <div class="d-flex flex-column text-white">
            <div class="p-2 h3 mb-4 border-bottom border-warning">HOME </div>
            <div class="main-scroll">  
            <% future_events.each do |event| %>
                <% event.each do |brief_hash|%>
                    <div class="p-2 h3 border-bottom border-warning">
                        <div class="block-bg">
                            <a class="link-block" href=<%== brief_hash.event_href %> id=<%== brief_hash.event_html_link_id %>>
                                <div class="card-header"><%== brief_hash.event_date %></div>
                                <div class="card-body">
                                <h5 class="card-title"><%== brief_hash.event_name %></h5>
                                <p class="card-text"><%== brief_hash.event_description %></p>
                            </a>
                            <footer class="text-center text-muted h5">
                                <div class="row">
                                    <div class="col share">
                                        <div class="align-bottom ">Comment</div>
                                    </div>
                                    <div class="col like d-inline align-bottom ">
                                        <div class="d-inline-block">Like</div>
                                    </div>
                                </div>
                            </footer>
                            </div>
                        </div>
                    </div>
                   
                <% end %>
            <% end %>                 
            </div>
        </div>
    </div>
    <div class="col-md-3 d-none d-sm-block" style="max-width: 20rem; width: 20rem;">
        <form class="mt-3" action="/events/search" method="get">
            <input class="form-control mr-sm-2" type="search" name="q" placeholder="Search" aria-label="Search">
        </form>
        <div class="mt-4 text-white h4 border-bottom">
            Event!!!
        </div>
        <div class="mt-3 home-top-scroll">
            <% future_events.each do |event| %>
                <% event.each do |brief_hash|%>
                <div class="card text-white bg-dark mb-3" >
                    <div class="card-header"><%== brief_hash.event_date %></div>
                    <div class="card-body">
                        <h5 class="card-title"><%== brief_hash.event_name %></h5>
                        <a href=<%== brief_hash.event_href %>  class="btn btn-outline-warning">more</a>
                    </div>
                </div>
                <% end %>
            <% end %>    
        </div>
    </div>

    